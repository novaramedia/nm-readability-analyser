{
  "name": "nm-readability-analyser",
  "version": "2.0.0",
  "private": true,
  "description": "WordPress plugin — readability analysis and read time estimation",
  "license": "GPL-3.0",
  "scripts": {
    "build": "esbuild src/admin.js --bundle --minify --outfile=dist/admin.js --target=es2020",
    "watch": "esbuild src/admin.js --bundle --outfile=dist/admin.js --target=es2020 --watch"
  },
  "dependencies": {
    "readability-scores": "^1.0.8"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
